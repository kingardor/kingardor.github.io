// CSS-only stand-in for the WebGL monolith: pre-canvas-load, mobile, and
// reduced-motion all land here. A faint obsidian slab silhouette with an
// ember seam — cheap, static, instantly painted.
export default function Poster() {
  return <div className="monolith-poster" aria-hidden="true" />
}
